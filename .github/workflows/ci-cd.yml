# CI/CD Pipeline for Smart Recipe Meal Planner Backend
# Triggers on push to main and pull requests for continuous integration
# Deploys to AWS EC2 only on push to main branch
name: Backend CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ==================== CI STAGE ====================
  # Runs on every push and pull request
  # Compiles code, runs tests, static analysis, and security scanning
  ci:
    name: Continuous Integration
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout source code from the repository (full history for SonarCloud blame analysis)
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Step 2: Set up Java 17 with Maven caching for faster builds
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # Step 3: Compile the project and run all unit tests
      - name: Build and Test
        run: mvn clean test -B

      # Step 4: Run static analysis (SpotBugs with FindSecBugs, PMD) and generate JaCoCo coverage
      - name: Run Static Analysis and Code Coverage
        run: mvn verify -B -DskipTests -Dspotbugs.failOnError=false -Dpmd.failOnViolation=false

      # Step 5: Run SonarCloud analysis for continuous code quality monitoring
      - name: SonarCloud Analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -DskipTests -Dsonar.projectKey=smart-recipe-meal-planner -Dsonar.organization=escthedeadline -Dsonar.host.url=https://sonarcloud.io

      # Step 6: Run Trivy vulnerability scanner on the project filesystem
      - name: Security Vulnerability Scan with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          output: 'trivy-report.txt'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '0'

      # Step 7: Upload SpotBugs analysis report as CI artifact
      - name: Upload SpotBugs Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: spotbugs-report
          path: target/spotbugs/

      # Step 8: Upload PMD analysis report as CI artifact
      - name: Upload PMD Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pmd-report
          path: target/pmd*

      # Step 9: Upload JaCoCo code coverage report as CI artifact
      - name: Upload JaCoCo Coverage Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jacoco-coverage-report
          path: target/site/jacoco/

      # Step 10: Upload Trivy security scan report as CI artifact
      - name: Upload Trivy Security Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-security-report
          path: trivy-report.txt

      # Step 11: Upload the packaged JAR as a build artifact for deployment
      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: target/*.jar

  # ==================== CD STAGE ====================
  # Runs only on push to main branch (not on PRs)
  # Deploys the backend JAR to AWS EC2 via SSH
  cd:
    name: Continuous Deployment
    runs-on: ubuntu-latest
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      # Step 1: Download the build artifact from CI stage
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: deploy/

      # Step 2: Stop existing service on EC2
      - name: Stop existing service on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo systemctl stop mealplanner || true
            sudo mkdir -p /opt/mealplanner
            sudo chown ubuntu:ubuntu /opt/mealplanner

      # Step 3: Copy JAR file to EC2 instance
      - name: Copy JAR to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "deploy/*.jar"
          target: "/opt/mealplanner/"
          strip_components: 1

      # Step 4: Start the application on EC2 using prod profile (DB config in application-prod.properties)
      - name: Start Application on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Update systemd service - prod profile loads application-prod.properties
            sudo tee /etc/systemd/system/mealplanner.service > /dev/null <<'SERVICE'
            [Unit]
            Description=Smart Recipe Meal Planner API
            After=network.target
            [Service]
            Type=simple
            User=ubuntu
            ExecStart=/usr/bin/java -jar /opt/mealplanner/meal-planner-api-1.0.0.jar --spring.profiles.active=prod
            Restart=always
            RestartSec=10
            [Install]
            WantedBy=multi-user.target
            SERVICE

            sudo systemctl daemon-reload
            sudo systemctl enable mealplanner
            sudo systemctl start mealplanner
            sleep 30

      # Step 5: Smoke test to verify deployment was successful
      - name: Smoke Test - Health Check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            for i in 1 2 3 4 5 6; do
              curl -sf http://localhost:8080/api/health && echo "Health check passed - deployment successful" && exit 0
              echo "Attempt $i: waiting for app to start..."
              sleep 15
            done
            echo "Health check failed after 90 seconds"
            exit 1
